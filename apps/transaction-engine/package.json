{
  "name": "transaction-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm exec tsx watch src/index.ts",
    "build": "pnpm exec tsc -p tsconfig.json",
    "lint": "pnpm exec eslint src --ext .ts",
    "start": "node dist/index.js",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/express": "^5.0.6"
  },
  "dependencies": {
    "@scaffhold/config": "workspace:*",
    "@scaffhold/shared-types": "workspace:*",
    "dotenv": "^18.0.3",
    "express": "^5.2.1"
  }
}
