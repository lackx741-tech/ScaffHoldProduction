{
  "name": "scaffhold-production",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@12.6.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:packages": "pnpm --filter @scaffhold/shared-types build && pnpm --filter @scaffhold/config build",
    "dev": "pnpm run build:packages && concurrently -k -n dashboard,orchestrator,scanner,tx-engine,compiler \"pnpm --filter admin-dashboard exec vite --host 0.0.0.0 --port 5173\" \"pnpm --filter orchestrator-api dev\" \"pnpm --filter scanner-service dev\" \"pnpm --filter transaction-engine dev\" \"pnpm --filter compilation-service dev\"",
    "lint": "pnpm -r lint && pnpm exec eslint tests --ext .ts",
    "test": "pnpm run build:packages && pnpm exec vitest run",
    "typecheck": "pnpm run build:packages && pnpm -r typecheck"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.6.2",
    "@types/supertest": "^7.2.1",
    "concurrently": "^10.0.5",
    "eslint": "^10.11.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.7",
    "globals": "^17.12.0",
    "supertest": "^7.3.0",
    "tsx": "^4.23.15",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.70.1",
    "vitest": "^5.0.1"
  }
}
