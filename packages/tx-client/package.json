{
  "name": "@scaffhold/tx-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json && pnpm exec esbuild src/browser-entry.ts --bundle --format=iife --global-name=ScaffHoldTx --platform=browser --target=es2020 --minify --outfile=dist/scaffhold-tx.min.js",
    "lint": "pnpm exec eslint src --ext .ts",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
